<table st-pipe="customPipe" st-table="listOfStaffs" class="table table-striped" >
  <thead>
    <tr>
      <th st-sort="name">Name</th>
      <th st-sort="admin_role">Role</th>
      <th></th>
      <th></th>
    </tr>
    <tr>
        <th>
            <input st-search="name" placeholder="Search for name" class="form-control input-sm  search-bar" type="text"/>
        </th>
        <th>
          <select st-search="admin_role" class="form-control input-sm">
              <option value="">All</option>
              <option ng-repeat="role in staff.roles" value="{{role}}">{{role}}</option>
          </select>
        </th>
        <th></th>
        <th></th>
    </tr>
  </thead>
  <tbody>
    <tr ng-repeat="staffElem in listOfStaffs">
      <td>{{staffElem.name}} {{staffElem.surname}}</td>
      <td><span ng-show="!staff.editmode[staffElem.id]">{{staffElem.admin_role}}</span>
        <select ng-model="staffElem.admin_role" ng-show="staff.editmode[staffElem.id]" class="user-role-select form-control input-sm">
          <option ng-repeat="role in staff.roles" value="{{role}}">{{role}}</option>
        </select>
      </td>
      <td>
        <a href="" ng-hide="staff.editmode[staffElem.id]" ng-click="staff.editmode[staffElem.id] = true"><i class="fa fa-pencil"></i></a></td>
      <td>
        <a href="" ng-hide="staff.editmode[staffElem.id]" ng-click="staff.remove(staffElem)"><i class="fa fa-times"></i></a>
        <a href="" ng-show="staff.editmode[staffElem.id]" ng-click="staff.update(staffElem)"><i class="fa fa-check"></i></a>
      </td>
    </tr>
  </tbody>
</table>
